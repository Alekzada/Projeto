let limite = 10;
let saldo = 20;

let resultado = (saldo < limite) ? 'Necessita Depositar' : 'Não necessita depósito';
console.log(resultado)

/*
Sintaxe:
let variável = (condição) ? //instrução se true : //instrução se falso 
*/